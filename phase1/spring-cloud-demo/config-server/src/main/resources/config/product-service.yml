server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# product-service 커스텀 설정
product:
  currency: "USD"
  discount-rate: 0.1
